services:
  backend:
    build: .
    container_name: beeyond_tech_typescript_backend
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    command: npm run dev
    environment:
      - CHOKIDAR_USEPOLLING=true
